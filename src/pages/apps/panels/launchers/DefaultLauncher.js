//TODO: 
// - add dropdown to top center to select launcher, ad below it the launcher specific configuration panel (if available)
// - get configured launcher from device, and set in dropdown

import * as React from "react";

function DefaultLauncher() {
    return (
        <></>
    );
}

export default DefaultLauncher;
